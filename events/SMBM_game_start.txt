namespace = smbm

country_event = {
	id = smbm.1
	title = "Prepare yourself"
	desc = "Prepare your defences for the hard cold realities of space. Not knowing what lurks out there it would be best to prepare your defenses. Be careful, your defensive bonuses will only last a few years."
	
	picture = GFX_evt_space_station
	show_sound = event_default
	
	fire_only_once = yes
	
	trigger = {is_ai = no}
	
	immediate = {
		add_modifier = {
			modifier = be_careful_out_there
			days = 3650
		}
	}

	option = {
		name = "OK"
	}
}

# Planet even that constantly checks if Crime Lord Deal is active and if so
# Randomly decides if this should cause a crime wave.
planet_event = {
	id = smbm.2
	hide_window = yes
	
	mean_time_to_happen = {
		months = 1
	}
	
	trigger = {
		has_modifier = crime_lord_deal
	}
	
	immediate {
		random_list = {
			1 = { planet_event = { id = smbm.3 } }
			99 = {
				# Do nothing
			}
		}
	}
}

planet_event = {
	id = smbm.3
	title = "Arrogant Crime Lords"
	desc = "The crime lords have become arrogant in their actions. They have pushed the boundaries of the deals we signed with them and are now frequently operating in its grey zones. This has unfortunately resulted in a crime wave."
	
	picture = GFX_evt_dark_alley
	show_sound = event_criminal_activity
	
	is_triggered_only = yes
	
	immediate = {
		add_modifier = { modifier = crime_wave days = 3600 }
		set_timed_planet_flag = {
			flag = planet_crime_flag
			days = 3600
		}
	}
	
	option = {
		name = "We should have known"
		tooltip = {
			add_modifier = { modifier = crime_wave days = 3600 }
		}
	}
}